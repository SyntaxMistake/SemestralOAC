# render.yaml
services:
  - type: web
    name: tic-tac-toe-3d-server
    runtime: python
    startCommand: python server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
    healthCheckPath: /
    autoDeploy: true